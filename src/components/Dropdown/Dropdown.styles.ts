import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  menu: {
    position: 'absolute',
    minWidth: '184px',
    display: 'flex',
    flexDirection: 'column',
    top: 'calc(100% + 10px)',
    right: 0,
  },
});
